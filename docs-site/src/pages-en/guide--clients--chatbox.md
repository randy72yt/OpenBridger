# Use OpenBridger with Chatbox

Choose OpenAI API or an OpenAI-compatible custom service in model settings.

<div class="config-card"><div><span>API host / Base URL</span><code>{{API_BASE_URL}}</code></div><div><span>API key</span><code>YOUR_API_KEY</code></div><div><span>Model</span><code>Exact ID from the console</code></div></div>

Add the model manually if automatic discovery is empty. Send a short message and confirm the request in OpenBridger Usage Logs.
