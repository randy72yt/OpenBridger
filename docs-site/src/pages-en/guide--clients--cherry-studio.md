# Use OpenBridger with Cherry Studio

Add a custom OpenAI-compatible provider named OpenBridger. Enter `{{API_BASE_URL}}`, a dedicated key, and the exact model ID copied from the console.

Enable the provider, send a short message, and confirm the call in Usage Logs. Do not append `/v1` or `/chat/completions` twice.
