# Use OpenBridger with CodeBuddy

When CodeBuddy offers Custom Model, OpenAI Compatible, or a custom API address, add OpenBridger with `{{API_BASE_URL}}`, a dedicated key, and the exact model ID.

Menu names vary between releases. A version that cannot override the Base URL cannot use this method.
