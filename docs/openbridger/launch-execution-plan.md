# OpenBridger 完整上线执行计划（Review 定稿）

定稿日期：2026-09-26（用户已 review 并批准，执行中途不再逐项请示；秘密轮换等关键步骤留痕）

## 已完成（不重复做）

协议 v1.0 定稿并写入生产、文档站上线、Nginx 边缘限流、渠道 #1 dddai（40 模型）、41 模型定价同步 + 2 个 tiered_expr 手工补写、真实调用 200 验证、邮件每日配额计数验证、生产回归 20/20、CI 三 job 全绿。

## 阶段 A：代码收口与发布基线

| # | 任务 | 完成标准 |
|---|---|---|
| A1 | 合并 PR #2 → main | main 包含全部 CI/测试/文档修复 |
| A2 | 写 `VERSION`（0.1.0）并打 tag `v0.1.0` | `/api/status` 返回该版本号 |
| A3 | 用 main 构建 rc 镜像并灰度替换生产容器（保留旧镜像 tag 作回滚） | 回归脚本 20/20 仍全过；旧 tag 留存可回切 |

## 阶段 B：秘密轮换与账号加固

| # | 任务 | 注意 |
|---|---|---|
| B1 | MySQL root 密码轮换 | 改 `SQL_DSN` 滚动重启，回归验证 |
| B2 | Turnstile Secret 轮换 | 已泄露两次；需 Cloudflare 后台或 API，执行时确认通道 |
| B3 | 删除测试令牌 `sk-31a7...`（agent-verify，unlimited） | 延至 G2 前删除（验收期间仍需要） |
| B4 | 管理员账号（id=1）绑定邮箱 + 开启 MFA/Passkey，恢复码离线保存 | MFA 交互需用户配合 |
| B5 | 核查 `SESSION_SECRET`/`CRYPTO_SECRET` 强度；默认批准：趁用户少轮换 | 轮换会踢全部会话下线 |

## 阶段 C：域名与邮件收尾

| # | 任务 | 完成标准 |
|---|---|---|
| C1 | 根域 SPF（+ DKIM/DMARC 若缺）TXT 记录 | 测试邮件不进垃圾箱 |
| C2 | 复核 CF 缓存规则：`/v1/*`、`/api/*` Bypass cache | 连续两次 `curl /api/status` 无 `cf-cache-status: HIT`；流式逐块下发 |
| C3 | 注册验证 + 找回密码全链路复测（AUTH-003/004） | 真实邮箱走通全流程 |

## 阶段 D：可靠性——备份、回滚、监控

| # | 任务 | 完成标准 |
|---|---|---|
| D1 | MySQL 定时 dump + 数据卷快照 + 异地拷贝（cron） | 备份文件真实生成并外传 |
| D2 | 恢复演练：临时环境用备份恢复并启动 | 恢复后服务可用、数据完整 |
| D3 | 回滚演练：切回上一个镜像 tag → 验证 → 切回 | 10 分钟内完成，数据无损 |
| D4 | 日志轮转 + 基础告警 + 日志脱敏复核（OBS-001） | 日志无密钥/完整 Key 明文 |

## 阶段 E：定价与计费验收（倍率调整归用户，Agent 只做验证链路）

| # | 任务 | 完成标准 |
|---|---|---|
| E1 | 动态定价同步连续 48h 观察：定时任务执行、报价未过期、warnings 可见 | 连续两次同步倍率与上游一致 |
| E2 | 毛利设置 → 生成方案 → 人工审核 → 发布，完整走一遍（BILL-003/004） | 前台售价与发布方案一致 |
| E3 | 上游账单对账（BILL-001）：Δusage×5000 比对本地扣费 | 偏差可解释 |
| E4 | 模型目录终验：定位 claude-opus 90s 超时（上游问题则隐藏）；全量模型逐个流式/非流式过一遍 | 未通过的隐藏，不用别名顶替 |

## 阶段 F：工程收尾

| # | 任务 | 决议（已批准） |
|---|---|---|
| F1 | fork 上 push/PR 事件不投递 Actions | 接受手动触发；ci.yml 加 `schedule` 每日兜底 |
| F2 | PG 安全用例兼容 | 降级为后续项（生产用 MySQL，矩阵已通过） |
| F3 | 刷新 launch-runbook.md / launch-plan.md 状态 | 上线全程同步更新 |

## 阶段 G：全量验收与试运行（最后闸门）

| # | 任务 | 完成标准 |
|---|---|---|
| G1 | 全量 P0 在 rc 上执行，填验收执行总表；失败项修复后重跑原用例 | 适用 P0 全过，签署完成 |
| G2 | 管理员发放试用额度流程验收（数值由用户定）；删除 B3 测试令牌 | 流程可发、可限、可撤 |
| G3 | 邀请制试运行 3–7 天：每日巡检（上游成功率、余额、毛利、错误率、反馈） | 巡检记录连续；无 P0 问题再谈扩大开放 |

## 执行顺序与硬阻断

A → B → C →（D、E 并行）→ F 穿插 → G。**B、C、D 是试运行前的硬阻断**；E 可延续到试运行初期。

## 待用户后续提供（不阻塞 A–F）

1. 试运行首批用户数、每人试用额度、单用户上限（G2 前给出即可）
2. B2 若需 Cloudflare 控制台操作，用户提供或授权 API token
3. B4 的 MFA/Passkey 绑定需用户在浏览器端配合
